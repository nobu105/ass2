<header>
<h1>Editing Book</h1>
<br>
<%= form_for(@book) do |f| %>

<% if @book && @book.errors.any? %>
    <%= @book.errors.count %>errors prohibited this obj from being saved:<br>
     <% @book.errors.full_messages.each do |message| %>
        <%= message %>
  <% end %>
<% end %>

  <p>title</p>
  <%= f.text_field :title %>
  <br><br>
  <p>body</p>
  <%= f.text_area :body %>
  <br><br><%= f.submit 'Update Book' %>
<% end %>
<br><%= link_to "Show", book_path(@book) %>|<%= link_to "Back",books_path %>
</header>
